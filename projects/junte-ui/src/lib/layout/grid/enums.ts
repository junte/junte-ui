export enum RowAlign {
  start = 'start',
  center = 'center',
  end = 'end',
  stretch = 'stretch'
}

export enum RowJustify {
  start = 'start',
  center = 'center',
  end = 'end',
  between = 'between',
  around = 'around',
  evenly = 'evenly'
}
