export enum PopoverPlacements {
  top = 'top',
  right = 'right',
  bottom = 'bottom',
  left = 'left'
}

export enum PopoverTriggers {
  hover = 'hover',
  click = 'click'
}

export enum PopoverPosition {
  absolute = 'absolute',
  fixed = 'fixed'
}
