export enum ModalDisplay {
  block = 'block',
  none = 'none'
}

export enum ModalClosedReason {
  default = 'default',
  user = 'user'
}

export enum ModalScheme {
  default = 'default',
  simple = 'simple',
  ghost = 'ghost',
  chat = 'chat'
}

export enum ModalState {
  hidden = 'hidden',
  visible = 'visible'
}
