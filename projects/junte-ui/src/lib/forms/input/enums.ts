export enum InputType {
  text = 'text',
  password = 'password',
  number = 'number',
  email = 'email'
}
export enum InputScheme {
  normal = 'normal',
  failed = 'failed',
  success = 'success'
}
export enum InputState {
  loading = 'loading',
  warning = 'warning',
  checked = 'checked'
}
