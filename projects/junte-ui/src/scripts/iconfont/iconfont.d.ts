import 'reflect-metadata';
export declare class Gulpfile {
    private check;
    private read;
    private create;
    styles(cb: any): void;
    createConfig(): Promise<void>;
    build(): string[];
}
